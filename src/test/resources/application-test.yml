spring:
  application:
    name: easylearn-pack

eureka:
  client:
    enabled: false

ribbon:
  eureka:
   enabled: false

feign:
  hystrix:
    enabled: false

easylearn-note:
  ribbon:
    listOfServers: localhost:8085

easylearn-pack:
  ribbon:
    listOfServers: localhost:8085